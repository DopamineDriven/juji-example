# import Partial from './partial.graphql'
# import PartialRemote from './partial.graphql'

query Viewer {
  viewer {
    ...Partial
    status
  }
}

query ViewerRemote {
  viewerRemote {
    ...PartialRemote
    status
  }
}